// Delayed phase: 3rd-party tags, analytics, chat widgets. Loaded ~3s after LCP,
// gated behind consent where applicable. Nothing here blocks user-perceived performance.
// import initAnalytics from './lib-analytics.js';
// initAnalytics();
