// Example delayed-phase analytics init. Keep this file the ONLY place 3rd-party
// analytics tags are loaded — see docs/07-analytics.md.
export default function init() {
  window.dataLayer = window.dataLayer || [];
  // e.g. inject Adobe Launch / GTM script tag here, after consent check.
}
